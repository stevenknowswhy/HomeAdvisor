// Minimal Svelte config: the frontend needs no preprocessor (Svelte 5 handles
// TS in script tags natively); this file just declares the project's Svelte
// root so vite-plugin-svelte stops warning about the missing default.
export default {};
