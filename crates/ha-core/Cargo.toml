[package]
name = "ha-core"
description = "Home Advisor domain types and the deterministic fail-closed policy router"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
semver = { version = "1", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
