[package]
name = "ha-cli"
description = "Home Advisor demo CLI: seeds a family, exercises the privacy gate, prints the receipt"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
ha-core = { path = "../ha-core" }

[lints]
workspace = true
